name: IITM TDS Test

on:
workflow_dispatch:

jobs:
test:
runs-on: ubuntu-latest

```
steps:
  - name: Checkout
    uses: actions/checkout@v4

  - name: 24f3004361@ds.study.iitm.ac.in
    run: echo "Hello IITM TDS"
```
